export type DestinationType = {
  id: string;
  name?: string;
  name_uz?: string;
  slug?: string;
  description?: string;
  image?: string;
  cover_image?: string;
  icon?: string;
  is_active?: boolean;
};